# WalletConnect — get a free project id at https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Vercel KV (Upstash) — auto-set when you connect KV in Vercel dashboard
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Cron secret — set in Vercel env; Vercel sends this header on cron jobs
CRON_SECRET=
